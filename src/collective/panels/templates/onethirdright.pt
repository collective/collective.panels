<div class="row"
     tal:define="portlets portlets | python: (1, '&larr; 2')"
     tal:condition="portlets">
  <tal:left condition="python: len(portlets) &gt; 1">
    <tal:loop define="start python: -98.875;
                      ratio python: round(66.667 / (len(portlets) - 1), 3);
                      width python: ratio - 2.25"
              repeat="portlet python: reversed(portlets[1:])">
      <div class="cell"
           tal:define="index repeat/portlet/index;
                       position python: start + ratio * index"
           tal:attributes="style string:width: ${width}%;; margin-left: ${position}%">
        <div tal:content="structure portlet" />
      </div>
    </tal:loop>
  </tal:left>

  <div class="cell position-0 width-full"
       tal:condition="python: len(portlets) == 1">
    <div tal:content="structure python: portlets[0]" />
  </div>

  <div class="cell position-2:3 width-1:3"
       tal:condition="python: len(portlets) &gt; 1">
    <div tal:content="structure python: portlets[0]" />
  </div>
</div>







